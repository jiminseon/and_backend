server:
  port: 8081
  forward-headers-strategy: framework

spring:
  application:
    name: user-service
  datasource:
    url: ${USER_DB_URL}
    username: ${DB_USERNAME:and}
    password: ${DB_PASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
  rabbitmq:
    host: ${RABBITMQ_HOST:rabbitmq}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME}
    password: ${RABBITMQ_PASSWORD}

jwt:
  private-key: ${JWT_PRIVATE_KEY:file:/run/secrets/jwt/private-key.pem}
  public-key: ${JWT_PUBLIC_KEY:file:/run/secrets/jwt/public-key.pem}
  issuer: ${JWT_ISSUER:and-user-service}
  audience: ${JWT_AUDIENCE:and-services}
  access-expiration-time: ${JWT_ACCESS_EXPIRATION_MS:3600000}
  refresh-expiration-time: ${JWT_REFRESH_EXPIRATION_MS:1209600000}

auth:
  cookie:
    secure: ${AUTH_COOKIE_SECURE:true}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

